import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1>Aluraflix</h1>
            <button>Nuevo Video</button>
        </header>
    );
};

export default Header;
